name: One-shot release Hermès 1.0.3

on:
  push:
    branches:
      - 'release/hermes-1.0.3'

permissions:
  contents: write

jobs:
  release:
    runs-on: windows-latest
    timeout-minutes: 25
    steps:
      - name: Checkout release source
        uses: actions/checkout@v4
        with:
          ref: 'cf0c612b1f7bd22034dfd3d4313a9f8d1cc28284'

      - name: Install .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Validate web client
        shell: pwsh
        run: node --check acars/wwwroot/app.js

      - name: Restore and build
        shell: pwsh
        run: |
          dotnet restore acars/Promethee.Acars.csproj
          dotnet build acars/Promethee.Acars.csproj -f net8.0-windows -c Release --no-restore

      - name: Install Inno Setup
        shell: pwsh
        run: choco install innosetup --yes --no-progress

      - name: Build installer
        shell: pwsh
        run: ./acars/build-installer.ps1 -Version '1.0.3'

      - name: Create tag and release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release delete hermes-v1.0.3 --repo "$env:GITHUB_REPOSITORY" --yes --cleanup-tag 2>$null
          git tag hermes-v1.0.3 cf0c612b1f7bd22034dfd3d4313a9f8d1cc28284
          git push origin refs/tags/hermes-v1.0.3
          gh release create hermes-v1.0.3 `
            "dist/Hermes-ACARS-Setup-1.0.3.exe" `
            "dist/Hermes-ACARS-Setup-1.0.3.exe.sha256" `
            "dist/Promethee-ACARS-win-x64-1.0.3.zip" `
            "dist/Promethee-ACARS-win-x64-1.0.3.zip.sha256" `
            --repo "$env:GITHUB_REPOSITORY" `
            --title "Hermès 1.0.3" `
            --verify-tag `
            --generate-notes
