@extends('promethee::layout')
@section('title','Missions et circuits')
@section('content')
<div class="ops-header compact"><div><span class="eyebrow">OPÉRATIONS SPÉCIALES</span><h1>Missions et circuits.</h1><p>Chaque étape est automatiquement validée par un PIREP accepté correspondant à la ligne.</p></div></div>
<section class="panel"><div class="panel-heading"><div><span class="eyebrow">MISSIONS</span><h2>À accomplir</h2></div></div><div class="flight-cards">@forelse($missions as $mission)<article class="panel line-card"><div class="line-card-head"><div><span class="eyebrow">{{ $mission->completion ? 'VALIDÉE' : 'EN COURS' }}</span><h2>{{ $mission->title }}</h2></div><span class="tag">{{ $mission->dpt_airport_id ?: 'Libre' }} → {{ $mission->arr_airport_id ?: 'Libre' }}</span></div><p>{{ $mission->description }}</p><small>{{ $mission->starts_on ?: 'Sans début' }} · {{ $mission->ends_on ?: 'Sans échéance' }}</small></article>@empty<p class="empty">Aucune mission active.</p>@endforelse</div></section>
<section class="panel"><div class="panel-heading"><div><span class="eyebrow">CIRCUITS</span><h2>Tours en plusieurs étapes</h2></div></div>@forelse($circuits as $circuit)<article class="panel"><div class="panel-heading"><div><span class="eyebrow">{{ $circuit->completed ? 'CIRCUIT TERMINÉ' : 'PROGRESSION' }}</span><h2>{{ $circuit->title }}</h2><p>{{ $circuit->description }}</p></div><span class="tag">{{ $circuit->legs->where('completion')->count() }}/{{ $circuit->legs->count() }}</span></div><div class="route-board">@foreach($circuit->legs as $leg)<article><b>{{ $leg->dpt_airport_id }}</b><i></i><b>{{ $leg->arr_airport_id }}</b><span>{{ $leg->completion ? 'Validée' : 'À voler' }}</span></article>@endforeach</div></article>@empty<p class="empty">Aucun circuit actif.</p>@endforelse</section>
@endsection
