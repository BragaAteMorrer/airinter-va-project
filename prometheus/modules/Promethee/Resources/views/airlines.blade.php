@extends('promethee::layout')
@section('title', 'Compagnies')
@section('content')
<div class="ops-header compact"><div><span class="eyebrow">AIR INTER · COMPAGNIE</span><h1>Nos compagnies.</h1><p>Les opérateurs actifs du réseau et leurs moyens d’exploitation.</p></div><span class="tag">{{ $airlines->count() }} active(s)</span></div>
<section class="panel"><form method="get" class="flight-filter"><label class="filter-wide">Rechercher<input name="q" value="{{ request('q') }}" placeholder="Nom, ICAO ou IATA"></label><button>Rechercher</button><a class="button outline" href="{{ route('promethee.airlines') }}">Réinitialiser</a></form></section>
<section class="panel table-wrap"><table><thead><tr><th>Logo</th><th>Compagnie</th><th>Codes</th><th>Flotte</th><th>Sous-flottes</th><th>Pilotes</th><th>Vols programmés</th></tr></thead><tbody>@forelse($airlines as $airline)<tr><td>@if($airline->promethee_logo)<img src="{{ $airline->promethee_logo }}" alt="Logo {{ $airline->name }}" style="width:64px;max-height:34px;object-fit:contain">@else<span class="tag">{{ $airline->icao }}</span>@endif</td><td><strong>{{ $airline->name }}</strong></td><td>{{ $airline->icao }}@if($airline->iata) · {{ $airline->iata }}@endif</td><td>{{ number_format($airline->aircraft_count) }}</td><td>{{ number_format($airline->subfleets_count) }}</td><td>{{ number_format($airline->users_count) }}</td><td>{{ number_format($airline->flights_count) }}</td></tr>@empty<tr><td colspan="7">Aucune compagnie active ne correspond à la recherche.</td></tr>@endforelse</tbody></table></section>
@endsection
