@extends('promethee::layout')
@section('title','Jumpseat')
@section('content')
<div class="ops-header compact"><div><span class="eyebrow">MOBILITÉ PILOTE</span><h1>Jumpseat.</h1><p>Déplacez immédiatement votre position pilote vers un autre aéroport, sans modifier votre hub ni votre compagnie.</p></div><div class="ops-clock"><span>Solde phpVMS</span><strong>{{ $wallet }}</strong><small>{{ number_format($basePrice, 2, ',', ' ') }} € / NM</small></div></div>
<section class="panel"><form method="post" action="{{ route('promethee.jumpseat.buy') }}" class="form-grid">@csrf<label>Aéroport de destination<select name="target_airport_id" required>@foreach($airports as $airport)<option value="{{ $airport->id }}">{{ $airport->icao ?: $airport->id }} · {{ $airport->name }} · {{ $airport->country }}</option>@endforeach</select></label><label class="full">Motif facultatif<textarea name="reason" rows="3"></textarea></label><div><button type="submit" name="preview" value="1">Calculer le tarif</button> <button type="submit">Acheter le jumpseat</button></div></form><p class="muted">@if($origin) Départ : {{ $origin->icao ?: $origin->id }}. @endif Tarif historique Prometheus : {{ number_format($basePrice, 2, ',', ' ') }} € × distance orthodromique en NM@if($discount > 0 && $discount < 100), avec {{ number_format($discount, 0) }} % de remise@endif. Le montant exact est calculé et débité immédiatement du journal phpVMS ; il est crédité à votre compagnie.</p></section>
<section class="panel table-wrap"><h2>Mes jumpseats</h2><table><thead><tr><th>Destination</th><th>Calcul</th><th>Date</th><th>État</th></tr></thead><tbody>@forelse($orders as $order)<tr><td>{{ $order->icao ?: '—' }} · {{ $order->airport_name }}</td><td>{{ $order->decision_note ?: '—' }}</td><td>{{ \Carbon\Carbon::parse($order->created_at)->format('d/m/Y') }}</td><td>{{ $order->status }}</td></tr>@empty<tr><td colspan="4">Aucun jumpseat.</td></tr>@endforelse</tbody></table></section>
@endsection
